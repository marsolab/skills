interface:
  display_name: "Mactidy"
  short_description: "Safely clean AI development leftovers on macOS"
  default_prompt: "Use $mactidy to audit this Mac for stale AI development leftovers and propose a safe cleanup plan."
policy:
  allow_implicit_invocation: true
